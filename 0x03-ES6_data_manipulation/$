export default function cleanSet(set, startString) {
    let result = '';
    for (const item of set) {
      const subItem = item.slice(0, startSting.length - 1);
      if (subItem === startString) {
	  result += result !== '' ? '- '.concat(item.slice(startSting.length - 1)) : item.slice(startSting.length - 1);
      }
    }
    return result;
}
